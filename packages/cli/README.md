# downword-cli

> Command line interface for [downword](https://www.npmjs.com/package/downword) —
> Markdown to Word (.docx).

<!-- TODO(README): placeholder. The full CLI README is written in a later task. -->

> **Status: T0 scaffold.** Only `--help` and `--version` do anything today;
> conversion is not wired into the CLI yet.

```sh
npm install -g downword-cli
downword --help
```

```
Usage:
  downword [options] <input.md>

Options:
  -o, --output <file>   Write the .docx here (default: alongside the input)
  -v, --version         Print the version and exit
  -h, --help            Print this help and exit
```

Source, issues and docs: <https://github.com/kspr-technologies/downword>

MIT © 2026 KSPR Technologies
