## Migration checklist

- [x] Audit the existing schema
- [x] Write the migration script
  - [x] `users` table
  - [ ] `orders` table
    - [ ] Backfill `orders.customer_id`
    - [x] Add the index
- [ ] Run it against staging
- [ ] Run it against production
- Not a checkbox at all, just a bullet
- [x]
- [X] Uppercase X counts as checked
- [] Missing space is not a checkbox
- [ ]No space after the bracket
- [x] Checked item with **bold** and `code` after the marker

1. [x] Ordered task item — keeps its number
2. [ ] Second ordered task item

* [ ] A task under an asterisk marker

  With a second paragraph, so the list is loose.
