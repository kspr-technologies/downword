1. First, read the warning:

   > **Warning**
   > This will drop the table. There is no undo.

   Then run:

   ```sql
   DROP TABLE orders_backup;
   ```

2. Second item with an indented code block instead of a fence:

       raw indented code
       second line

3. Third item containing a table:

   | Step | Owner |
   | ---- | ----- |
   | one  | you   |

4. Fourth item containing a nested quote containing a list:

   > - quoted bullet
   > - another
   >
   > > and a nested quote inside the quote
