Project Overview
================

This is the intro paragraph under a setext h1.

Why it exists
-------------

Setext h2, followed by a paragraph.

Not a heading, just a paragraph
followed by a lazy continuation line.

---

Ambiguous
---

The `---` above closes a setext h2, but the standalone one before it is a
thematic break.

A setext heading with `code` and **bold**
=========================================
