| Command | What it does | Docs |
| --- | --- | --- |
| `npm ci` | **Clean** install from the lockfile | [npm docs](https://docs.npmjs.com/cli/v10/commands/npm-ci) |
| `npm run build` | Runs the *build* script, then `postbuild` | [scripts](https://docs.npmjs.com/cli/v10/using-npm/scripts "npm scripts reference") |
| `grep -E 'a\|b'` | Escaped pipe inside inline code | — |
| a \| b | Escaped pipe in plain text | ~~deprecated~~ |
| ![icon](icon.png) | A cell containing an image | <https://example.com/auto> |
| Multi **bold** and `code` and [link](#anchor) in one cell | Everything at once | |

Table immediately followed by a paragraph, with no blank line in between.
