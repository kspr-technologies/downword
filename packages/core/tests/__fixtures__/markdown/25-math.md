The loss is $\mathcal{L} = -\sum_i y_i \log \hat{y}_i$, minimised by gradient
descent.

Display form:

$$
\mathcal{L}(\theta) = \frac{1}{N} \sum_{i=1}^{N} \ell(f_\theta(x_i), y_i)
$$

Math inside emphasis: **$E = mc^2$** and *$\alpha + \beta$*.

Math inside a list:

1. Compute $z = Wx + b$
2. Apply $\sigma(z) = \frac{1}{1 + e^{-z}}$

Math inside a table cell:

| Symbol | Meaning |
| ------ | ------- |
| $\eta$ | learning rate |
| $\nabla_\theta$ | gradient |

A dollar sign that is not math: it costs $5 and $10.

Math inside a link: [see $\pi$](https://example.com).
